import ChatWidget from "@/components/ChatWidget";

export default function WidgetPage() {
  return (
    <main className="min-h-screen bg-transparent">
      {/* This page is intentionally empty except the floating widget */}
      <ChatWidget />
    </main>
  );
}